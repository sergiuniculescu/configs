[core]
color = blue
align = top
display_modules = distro(), uname(n), uname(r), uname(p), uptime(), wm(), de(), packages(), env(shell), env(term), env(editor), \*env(browser)*/, fs(/boot), fs(/), fs(/home), ram(), mpd(albums)

